import { resolve } from 'path';

export const config = resolve('./src/db/config/config.json');
export const modelsPath = resolve('./src/db/models');
export const migrationsPath = resolve('./src/db/migrations');
